# TODO

## 消息处理增强

- [ ] 支持图片/文件消息 — 将图片转发给支持视觉的模型，或提取文件内容后发给 Agent
- [ ] 长回复分段发送 — 超长回复自动拆分多条消息，替代截断
- [x] Markdown 渲染 — Agent 回复转为飞书富文本卡片

## 交互体验

- [x] 流式回复 — Agent 生成过程中逐步更新消息
- [x] 回复引用 — 用 reply 方式回复原消息，保持对话关系
- [x] 取消请求 — `/cancel` 中断当前 Agent 调用

## 多用户/多场景

- [ ] 按用户隔离会话 — 群聊中按 user_id 而非 chat_id 隔离上下文
- [ ] Agent 预设角色 — `/role <name>` 设定系统提示词
- [x] 工作目录切换 — `/workspace <path>` 指定 Codex 工作目录

## 运维管理

- [ ] 白名单/黑名单 — 限制可使用机器人的用户或群
- [ ] 速率限制 — 防止单用户短时间大量请求
- [ ] 消息队列 — 并发处理多条消息，替代串行

## 已完成

- [x] 斜杠命令系统（`/help`、`/status`、`/agent`、`/new`）
- [x] 未知命令拦截
- [x] 多轮对话上下文保持
- [x] 会话超时配置
- [x] Emoji 反馈（处理中/出错）
- [x] 错误重试机制
- [x] systemd 服务管理
